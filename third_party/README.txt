Limine third_party placeholder.

This directory will contain Limine binaries/config once vendored.
For the first milestone, the build pipeline will generate Limine config and use a minimal bootloader payload strategy.

